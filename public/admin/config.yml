# ─── Backend ─────────────────────────────────────────────────
backend:
  name: github
  repo: Hauzknecht/nanofiber-web
  branch: main

media_folder: "public/images/blog"
public_folder: "/images/blog"
site_url: https://nanofiber-web.pages.dev

# ─── Shared field definitions (YAML anchors) ─────────────────
# These are reused in both EN and CS collections below
x-blog-fields: &blog-fields
  - label: "Title"
    name: "title"
    widget: "string"

  - label: "Publish Date"
    name: "date"
    widget: "datetime"
    format: "YYYY-MM-DD"
    date_format: "DD.MM.YYYY"
    time_format: false

  - label: "Short Summary (shown in blog list)"
    name: "excerpt"
    widget: "string"

  - label: "Category"
    name: "tag"
    widget: "select"
    options:
      - { label: "Research / Výzkum",    value: "Research"  }
      - { label: "Technical / Technické", value: "Technical" }
      - { label: "Industry / Průmysl",   value: "Industry"  }
      - { label: "News / Aktuality",     value: "News"       }

  - label: "Author"
    name: "author"
    widget: "string"
    default: "Jana Nováková"

  - label: "Translation slug (slug of the matching post in the other language)"
    name: "translationSlug"
    widget: "string"
    required: false
    hint: "If the other-language version of this post has a different filename, enter it here. Usually you can leave this blank."

  - label: "Cover Image"
    name: "coverImage"
    widget: "image"
    required: false

  - label: "Save as Draft (hide from site)"
    name: "draft"
    widget: "boolean"
    default: false

  - label: "Article Body"
    name: "body"
    widget: "markdown"

# ─── Collections ─────────────────────────────────────────────
collections:
  - name: "blog-en"
    label: "🇬🇧 Blog — English"
    label_singular: "English Post"
    folder: "src/content/blog-en"
    create: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    fields: *blog-fields

  - name: "blog-cs"
    label: "🇨🇿 Blog — Česky"
    label_singular: "Český příspěvek"
    folder: "src/content/blog-cs"
    create: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    fields: *blog-fields
